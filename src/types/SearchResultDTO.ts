// Matches backend response from GET /search?query={q}
export interface SearchResultDTO {
  symbol: string;
  name: string;
  type: string;
  region: string;
  marketOpen: string;
  marketClose: string;
  timezone: string;
  currency: string;
  matchScore: number;
}
